{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Discord Bot",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/discord-bot"
      }
    },
    {
      "label": "Run Backend",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/backend"
      }
    }
  ]
}
