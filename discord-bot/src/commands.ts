import * as ping from "./commands/ping";
import * as checkserver from "./commands/check-server";
import * as checkinit from "./commands/check-init";
import * as createserver from "./commands/create-server";

export const commands = {
  ping,
  checkserver,
  checkinit,
  createserver,
};
